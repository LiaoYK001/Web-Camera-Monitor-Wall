# 反馈5 持续执行检查点 / Feedback 5 progress checkpoint

更新 / Updated: 2026-09-20（证据复核第 2 轮 / evidence-review round 2）

## 当前代码 / Current code

- 上一提交 `ae292ee`（验收判定器修复 + 20 条回归）。本轮修复控制面串行激活：`core/src/control_server.cpp` 新增有界工作池 `RequestPool`（请求处理移出唯一 io_context 线程，仅响应写回投回会话 executor）、按来源路由锁（替换全局 `route_operation_mutex_`，锁内不做网络等待）、`WebSocketHub` 补锁、关闭顺序改为“停止接入 → 排空工作池 → 停 io_context → join”。`ctest` 通过（`webobs-unit-tests`）。
- 报告新增 6.1 节（含修复前后对照），6.2 节保留此前两个缺陷记录。

## 本轮完成：控制面串行激活修复 / Control-plane serial activation fixed

受控慢来源（本地 TCP 端点接受连接但永不回应，只替换一路相机端点，不动用户设备）：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `GET /api/v1/scene` >1 s 的采样 | 16 / 102（最大 10.12 s） | **0 / 248（最大 0.103 s）** |
| 五个并发 WHEP 耗时 | 10.2 / 10.3 / 20.3 / 20.4 / 20.4 s | 四路健康 0.157 / 0.371 / 0.380 / 0.382 s；只有慢来源自己 10.1 s |
| 五路出图 | 2–3 路 live | **4 路 515 ms live**，慢来源停在 connecting |
| 重复 activate | — | 5 路 = 5 条 `direct-`，1 条 `hybrid-`（同一计划重试 7 次后仍如此） |
| 优雅停止 | — | 5.31 s 完成，无挂起 |

## 下一条具体动作 / Next concrete steps

1. 真实来源证据：低竞争 + 明确 RTSP 传输下测输入帧率/关键帧间隔/损坏与恢复，并与经网关后的结果对比；给出需要用户决定的设备参数（如相机 slice/关键帧间隔）而不擅自修改。
2. 编码修复适用范围：补 HEVC→H264 Hybrid 路径验证（确认相机走的正是修复覆盖的链路）；NVENC/VA-API 仅在可用时做短对照。
3. 固定新 SHA 后按顺序重跑：受控五路 1800 秒、真实五路 1800 秒、原始 Composite 1800 秒、真实来源单路故障注入（判定器修复后）。

## 六项状态 / Status

| 项目 | 状态 | 证据 |
|---|---|---|
| F5-01 画面填充 | 已实现并自动化验证 | 聚焦套件 22/22 |
| F5-02 干净画面 | 已实现并自动化验证 | 聚焦套件 22/22 |
| F5-03 硬件加速 | 渲染 D3D12 已验证；NVENC 吞吐低于 x264，已按实测默认回退；NVENC/VA-API 转码分支 slice 行为未验证 | 报告第 2、4.4.2 节 |
| F5-04 本地合成 | presented 与首帧在原始规格达标；正式 30 分钟按修复后判定器为 **INCOMPLETE** | 报告 4.2.1 复核修正 |
| F5-05 音频管理 | 批次 A/B/C 已提交并有单测；音频回归三项全绿；视频位移与客户端侧相对偏移均实测通过 | 报告第 5 节 |
| F5-06 播放稳定 | **受控来源性能验证通过；原始部署最终验收尚待关闭**；控制面串行激活已修复（6.1），真实相机首帧/停顿仍待实测 | 报告 0、1.1、4.5、6.1 |

## 环境与阻塞 / Environment and blockers

- 开发栈当前**已停止**（本轮为重启到新二进制而优雅停止，5.31 s，端口全关）。合成源 fixture（`synth-hold2` + `synth3-hold`）与 Vite 仍在运行，`cameras.db` 仍临时指向合成源（含一路慢端点测试残留）。
- 收尾必须还原并校验：`cameras.db` sha256 `ac028d59…`、`scene.json` sha256 `87fcca32…`、`studio.json` 5 来源 5 项。
